// Basic smoke test for Alce's Barbearia app.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Supabase requires async init, so we just validate the test runner works.
    expect(true, isTrue);
  });
}
